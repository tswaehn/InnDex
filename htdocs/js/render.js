
function render_status( data, log ){
    document.getElementById("test1").innerHTML = log + data;
}

function render_config( data, log ){
    document.getElementById("test2").innerHTML = log + data;
}
